/* Core */
import React from 'react';
import ReactDOM from 'react-dom';

/* Theme */
import './theme/index.scss';

ReactDOM.render(
  <main className="starting-point">
    <h1
      style={{
        textAlign: 'center',
      }}>
      К старту готов ✅
    </h1>
  </main>,
  document.getElementById('root'),
);
